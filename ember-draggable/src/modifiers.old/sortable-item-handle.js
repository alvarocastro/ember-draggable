import { run } from '@ember/runloop';
import { modifier } from 'ember-modifier';

export default modifier(function sortableItemHandle (element, positional, named) {

  return () => {

  };
}, { eager: false });
